import BASE_URL from "./api";

const API = {

  // =========================================================
  // AUTHENTICATION
  // =========================================================

  LOGIN: `${BASE_URL}/Auth/login`,

  REGISTER: `${BASE_URL}/Auth/register`,

  FORGOT_PASSWORD: `${BASE_URL}/Auth/forgot-password`,


  // =========================================================
  // LOAN PLAN
  // =========================================================

  LOAN_PLAN: `${BASE_URL}/LoanPlans`,

  LOAN_PLAN_BY_ID: (id) =>
    `${BASE_URL}/LoanPlans/${id}`,


  // =========================================================
  // BRANCH
  // =========================================================

  BRANCH: `${BASE_URL}/branch`,


  // =========================================================
  // STAFF BRANCH
  // =========================================================

  STAFF_BRANCH: `${BASE_URL}/staff-branch`,


  // =========================================================
  // BRANCH CENTER
  // =========================================================

  BRANCH_CENTER: `${BASE_URL}/branch-center`,


  // =========================================================
  // GROUP
  // =========================================================

  GROUP: `${BASE_URL}/group`,
};

export default API;